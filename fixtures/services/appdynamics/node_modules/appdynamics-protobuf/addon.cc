#include <node.h>
#include <nan.h>
#include "protobuf_for_node.h"

NAN_MODULE_INIT(init)
{
  Nan::Set(target, Nan::New<v8::String>("Schema").ToLocalChecked(),
              protobuf_for_node::SchemaConstructor());
}

NODE_MODULE(protobuf_for_node, init)
